spring:
  application:
    name: USER_SERVICE-PROD

eureka:
  instance:
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        default-zone: http://localhost:8761/eureka
